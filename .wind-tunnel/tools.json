{
	"version": 1,
	"tools": [
		{
			"name": "apply_patch",
			"description": "Apply a model-native multi-file patch with repository-boundary and symbolic-link protection.",
			"entrypoint": ".wind-tunnel/tools/nazare.ts",
			"operation": "apply_patch",
			"timeoutMs": 3000,
			"maxOutputBytes": 24000,
			"parameters": {
				"patch": {
					"type": "string",
					"description": "Patch using *** Begin Patch, *** Update/Add/Delete File headers, @@ hunks, and *** End Patch"
				}
			}
		},
		{
			"name": "nazare_find",
			"description": "Search the compiled Nazare architecture registry for capabilities, surfaces, providers, and source boundaries.",
			"entrypoint": ".wind-tunnel/tools/nazare.ts",
			"operation": "find",
			"timeoutMs": 3000,
			"maxOutputBytes": 24000,
			"parameters": {
				"query": {
					"type": "string",
					"description": "Behavior, capability, surface, or provider to find"
				},
				"kind": {
					"type": "string",
					"description": "Optional registry kind: capability, carcass, or provider",
					"optional": true
				}
			}
		},
		{
			"name": "nazare_inspect",
			"description": "Inspect one registry entity or expand its directly connected executable neighborhood.",
			"entrypoint": ".wind-tunnel/tools/nazare.ts",
			"operation": "inspect",
			"timeoutMs": 3000,
			"maxOutputBytes": 24000,
			"parameters": {
				"id": { "type": "string", "description": "Exact registry entity ID" },
				"expand": {
					"type": "boolean",
					"description": "Include connected entities and bindings",
					"optional": true
				}
			}
		},
		{
			"name": "nazare_compile",
			"description": "Compile a requested capability change into source files, bindings, policies, evidence, and verification requirements.",
			"entrypoint": ".wind-tunnel/tools/nazare.ts",
			"operation": "compile",
			"timeoutMs": 3000,
			"maxOutputBytes": 24000,
			"parameters": {
				"capabilityId": {
					"type": "string",
					"description": "Exact capability ID"
				},
				"requestedChange": {
					"type": "string",
					"description": "Requested behavior change"
				}
			}
		}
	]
}
