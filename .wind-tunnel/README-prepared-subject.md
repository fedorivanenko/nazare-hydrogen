# Prepared subject contract

`subject.json` describes how the generic Wind Tunnel can prepare this repository once per immutable dependency key and compile reusable subject metadata.

The compiled metadata is intentionally subject-owned: Nazare understands its own capabilities, carcass, providers, bindings, policies, and source boundaries; the Wind Tunnel only understands preparation, isolation, caching, mutation, verification, and telemetry.
